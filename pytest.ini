[pytest]
minversion = 7.0
addopts = -ra -q --strict-markers
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    integration: mark a test as integration test requiring real API